diff --git a/src/manifest-store.js b/src/manifest-store.js
index 23225d17a41f0dfc562748e948ca3bc0f9d82e7e..04829cd98f95a67021c75772a33b0b69d8aae035 100644
--- a/src/manifest-store.js
+++ b/src/manifest-store.js
@@ -20,12 +20,12 @@ const ManifestStore = async ({ ipfs, storage } = {}) => {
     await IPFSBlockStorage({ ipfs, pin: true })
   )
 
-  const get = async (address) => {
+  const get = async (address, signal) => {
     const bytes = await storage.get(address)
     const { value } = await Block.decode({ bytes, codec, hasher })
     if (value) {
       // Write to storage to make sure it gets pinned on IPFS
-      await storage.put(address, bytes)
+      await storage.put(address, bytes, signal)
     }
     return value
   }
diff --git a/src/oplog/log.js b/src/oplog/log.js
index 0bddde6cd874580e9562e9c77a0d5bf98eb405a2..f3b1febc9a1be59427192787c7c12c377608eedd 100644
--- a/src/oplog/log.js
+++ b/src/oplog/log.js
@@ -298,12 +298,12 @@ const Log = async (identity, { logId, logHeads, access, entryStorage, headsStora
 
       await traverseAndVerify()
 
+      /* 7. Add the new entry to heads (=union with current heads) */
+      await oplogStore.addHead(entry)
       /* 4. Add missing entries to the oplog store (=to the log) */
       await oplogStore.addVerified(hashesToAdd.values())
       /* 6. Remove heads which new entries are connect to */
       await oplogStore.removeHeads(connectedHeads.values())
-      /* 7. Add the new entry to heads (=union with current heads) */
-      await oplogStore.addHead(entry)
 
       return true
     }
diff --git a/src/oplog/oplog-store.js b/src/oplog/oplog-store.js
index fb4a732274e056e1adf499b685b5a34cc304ade5..1b56421626f15271762182adb78b6cafc0eee341 100644
--- a/src/oplog/oplog-store.js
+++ b/src/oplog/oplog-store.js
@@ -59,6 +59,10 @@ const OplogStore = async ({ logHeads, entryStorage, headsStorage, indexStorage,
   }
 
   const addHead = async (entry) => {
+    // Add entry to the entry storage
+    const { hash, bytes } = await Entry.encode(entry, encryptEntryFn, encryptPayloadFn)
+    await _entries.put(hash, bytes)
+
     /* 7. Add the new entry to heads (=union with current heads) */
     await _heads.add(entry)
     return entry.hash
diff --git a/src/orbitdb.js b/src/orbitdb.js
index cc055cb011aa58bd77698694423d1ff88aa0ab2d..1d6f428ccee6092434cd1f11b7aaa5c700b148b2 100644
--- a/src/orbitdb.js
+++ b/src/orbitdb.js
@@ -106,6 +106,8 @@ const OrbitDB = async ({ ipfs, id, identity, identities, directory } = {}) => {
    * use for [Log]{@link module:Log} entries.
    * @param {number} [params.encryption] Encryption module to encrypt/decrypt database payloads and entries. If provided, the encryption object must take the form { replication: { encrypt, decrypt }, data: { encrypt, decrypt } }.
    * @memberof module:OrbitDB
+   * @param {AbortSignal} [params.signal] Optional abort signal.
+   * @memberof module:OrbitDB
    * @return {module:Database} A database instance.
    * @throws "Unsupported database type" if the type specified is not in the list
    * of known databaseTypes.
@@ -113,7 +115,7 @@ const OrbitDB = async ({ ipfs, id, identity, identities, directory } = {}) => {
    * @instance
    * @async
    */
-  const open = async (address, { type, meta, sync, Database, AccessController, headsStorage, entryStorage, indexStorage, referencesCount, encryption } = {}) => {
+  const open = async (address, { type, meta, sync, Database, AccessController, headsStorage, entryStorage, indexStorage, referencesCount, encryption, signal } = {}) => {
     let name, manifest, accessController
 
     if (databases[address]) {
@@ -123,7 +125,7 @@ const OrbitDB = async ({ ipfs, id, identity, identities, directory } = {}) => {
     if (isValidAddress(address)) {
       // If the address given was a valid OrbitDB address, eg. '/orbitdb/zdpuAuK3BHpS7NvMBivynypqciYCuy2UW77XYBPUYRnLjnw13'
       const addr = OrbitDBAddress(address)
-      manifest = await manifestStore.get(addr.hash)
+      manifest = await manifestStore.get(addr.hash, signal)
       const acType = manifest.accessController.split('/', 2).pop()
       AccessController = getAccessController(acType)()
       accessController = await AccessController({ orbitdb: { open, identity, ipfs }, identities, address: manifest.accessController })
diff --git a/src/storage/composed.js b/src/storage/composed.js
index 5c35309fd13700c1aa96818cbe9fc663c315e873..cb6e1785b24b25c6bda38075bbb011fa2cc42ed3 100644
--- a/src/storage/composed.js
+++ b/src/storage/composed.js
@@ -27,12 +27,13 @@ const ComposedStorage = async (storage1, storage2) => {
    * @function
    * @param {string} hash The hash of the data to put.
    * @param {*} data The data to store.
+   * @param {AbortSignal} signal An optional abort signal.
    * @memberof module:Storage.Storage-Composed
    * @instance
    */
-  const put = async (hash, data) => {
-    await storage1.put(hash, data)
-    await storage2.put(hash, data)
+  const put = async (hash, data, signal) => {
+    await storage1.put(hash, data, signal)
+    await storage2.put(hash, data, signal)
   }
 
   /**
@@ -43,15 +44,16 @@ const ComposedStorage = async (storage1, storage2) => {
    * storage2 but not in storage1, the data is added to storage1.
    * @function
    * @param {string} hash The hash of the data to get.
+   * @param {AbortSignal} signal An optional abort signal.
    * @memberof module:Storage.Storage-Composed
    * @instance
    */
-  const get = async (hash) => {
-    let value = await storage1.get(hash)
+  const get = async (hash, signal) => {
+    let value = await storage1.get(hash, signal)
     if (!value) {
-      value = await storage2.get(hash)
+      value = await storage2.get(hash, signal)
       if (value) {
-        await storage1.put(hash, value)
+        await storage1.put(hash, value, signal)
       }
     }
     return value
diff --git a/src/storage/ipfs-block.js b/src/storage/ipfs-block.js
index 44dfd68c7cdcf0defce4d6b72f202e46c2778c74..a766e9efe394448ba679b7b96bed486076bec6cb 100644
--- a/src/storage/ipfs-block.js
+++ b/src/storage/ipfs-block.js
@@ -8,6 +8,7 @@ import { CID } from 'multiformats/cid'
 import { base58btc } from 'multiformats/bases/base58'
 import { TimeoutController } from 'timeout-abort-controller'
 import drain from 'it-drain'
+import { anySignal } from 'any-signal'
 
 const DefaultTimeout = 30000 // 30 seconds
 
@@ -35,13 +36,19 @@ const IPFSBlockStorage = async ({ ipfs, pin, timeout } = {}) => {
    * @function
    * @param {string} hash The hash of the block to put.
    * @param {*} data The data to store in the IPFS block.
+   * @param {AbortSignal} signal An optional abort signal.
    * @memberof module:Storage.Storage-IPFS
    * @instance
    */
-  const put = async (hash, data) => {
+  const put = async (hash, data, signal) => {
     const cid = CID.parse(hash, base58btc)
-    const { signal } = new TimeoutController(timeout || DefaultTimeout)
-    await ipfs.blockstore.put(cid, data, { signal })
+    const { signal: timeoutSignal } = new TimeoutController(timeout || DefaultTimeout)
+    const combinedSignal = anySignal([
+      timeoutSignal,
+      ...(signal ? [signal] : [])
+    ])
+    timeoutControllers.add(combinedSignal)
+    await ipfs.blockstore.put(cid, data, { signal: combinedSignal })
 
     await persist(hash)
   }
@@ -52,16 +59,26 @@ const IPFSBlockStorage = async ({ ipfs, pin, timeout } = {}) => {
    * Gets data from an IPFS block.
    * @function
    * @param {string} hash The hash of the block to get.
+   * @param {AbortSignal} signal An optional abort signal.
    * @return {Uint8Array} The block.
    * @memberof module:Storage.Storage-IPFS
    * @instance
    */
-  const get = async (hash) => {
+  const get = async (hash, signal) => {
     const cid = CID.parse(hash, base58btc)
     const controller = new TimeoutController(timeout || DefaultTimeout)
     timeoutControllers.add(controller)
-    const block = await ipfs.blockstore.get(cid, { signal: controller.signal })
+    const combinedSignal = anySignal([
+      controller.signal,
+      ...(signal ? [signal] : [])
+    ])
+    timeoutControllers.add(combinedSignal)
+
+    const block = await ipfs.blockstore.get(cid, { signal: combinedSignal })
+
     timeoutControllers.delete(controller)
+    timeoutControllers.delete(combinedSignal)
+
     if (block) {
       return block
     }
